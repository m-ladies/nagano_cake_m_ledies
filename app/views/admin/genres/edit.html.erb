<h2 class="head-line title_h2 text-center" >ジャンル編集</h2>
<%= form_with url:edit_admin_genre_path(@genre.id), model:@genre, local:true do |f| %>
   <div>
     ジャンル
     <%= f.text_field :name  %>
     <%= f.submit "変更を保存", class: "btn btn-success" %>
   </div>
 <% end %>