<div class="mt-3 d-flex align-items-center">
  <h6 class="d-inline-block mb-0">ジャンル検索</h6>
    <div class="d-inline-block ml-3">
      <%= form_with url: customers_search_path, method: :get, local: true do |f| %>
        <%= f.collection_select :genre_id, Genre.all, :id, :name, prompt: "すべて", required: "required" %>
        <%= f.submit 'Search', class: "fas fa-search faa-pulse animated-hover search-icon,btn btn-outline-success" %>
      <% end %>
    </div>
</div>

